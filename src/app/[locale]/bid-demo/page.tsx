"use client"

import Link from "next/link"
import { useEffect, useState } from "react"
import { ArrowLeft, ArrowRight, CheckCircle2, Clock3, FileLock2, LockKeyhole, Radar, ShieldCheck, TimerReset } from "lucide-react"

import { BrandMark } from "@/components/brand-mark"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Textarea } from "@/components/ui/textarea"
import { isLocale, type Locale } from "@/lib/i18n"

const copy = {
  zh: { back: "返回登入", preview: "DESIGN PREVIEW", mission: "SEALED BID MISSION", title: "最後窗口：在倒數結束前封存你的報價。", route: "Ho Chi Minh City → Hong Kong", deadline: "距離密封窗口結束", final: "FINAL WINDOW", cargo: "貨物資料", services: "所需服務", quote: "報價控制台", amount: "報價金額（HKD）", transit: "預計運輸時間", terms: "服務條款與備註", token: "示範模式，不會扣除 Token", launch: "啟動密封傳輸", confirm: "確認封存報價", edit: "返回編輯", sealed: "競爭對手的價格、身份與條款均會保持密封。", success: "報價已封存", successBody: "這是設計示範。真實競價時，系統會記錄提交時間並在截標後通知結果。" },
  en: { back: "Back to sign in", preview: "DESIGN PREVIEW", mission: "SEALED BID MISSION", title: "Final window: seal your quote before the timer reaches zero.", route: "Ho Chi Minh City → Hong Kong", deadline: "Time remaining in bid window", final: "FINAL WINDOW", cargo: "Cargo", services: "Services", quote: "Quote console", amount: "Bid amount (HKD)", transit: "Estimated transit time", terms: "Terms and remarks", token: "Preview mode. No Token will be used.", launch: "Seal and transmit bid", confirm: "Confirm sealed bid", edit: "Back to edit", sealed: "Competitor prices, identities and terms remain sealed.", success: "Bid sealed", successBody: "This is a design preview. In a real bid, LBID records the submission time and notifies you after the window closes." },
}

export default function BidDemoPage({ params }: { params: { locale: string } }) {
  const locale: Locale = isLocale(params.locale) ? params.locale : "en"; const t = copy[locale]
  const [remaining, setRemaining] = useState(14 * 60 + 28); const [amount, setAmount] = useState("12800"); const [transit, setTransit] = useState(locale === "zh" ? "3 至 5 天" : "3 to 5 days"); const [terms, setTerms] = useState(locale === "zh" ? "包括香港本地派送、文件處理及清關協調。" : "Includes Hong Kong local delivery, documents and customs coordination."); const [confirming, setConfirming] = useState(false); const [sealed, setSealed] = useState(false)
  useEffect(() => { const timer = window.setInterval(() => setRemaining((value) => Math.max(0, value - 1)), 1000); return () => window.clearInterval(timer) }, [])
  const minutes = Math.floor(remaining / 60); const seconds = remaining % 60; const timer = `00:${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`
  return <main className="bid-mode-page min-h-screen"><header className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4 sm:px-6"><BrandMark markClassName="h-9 w-[138px]" /><div className="flex items-center gap-3"><Badge variant="gold">{t.preview}</Badge><Link href={`/${locale}/auth`} className="text-sm font-semibold text-lblue hover:underline"><ArrowLeft className="mr-1 inline h-4 w-4" />{t.back}</Link></div></header><section className="bid-mode-page mx-auto w-full max-w-6xl px-4 pb-20 pt-4 sm:px-6"><section className="bid-mission bid-mission-final"><div className="bid-grid" /><div className="relative z-10 flex flex-col gap-6 p-6 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-2xl"><div className="flex flex-wrap gap-2"><Badge variant="gold">{t.mission}</Badge><Badge variant="teal">BIDDING OPEN</Badge><Badge variant="secondary">{t.final}</Badge></div><h1 className="mt-5 text-3xl font-semibold tracking-tight text-white sm:text-5xl">{t.route}</h1><p className="mt-3 text-base leading-7 text-slate-300">{t.title}</p></div><div className="bid-countdown"><div className="flex items-center justify-between text-[11px] font-bold tracking-[.14em] text-[#dcc36e]"><span>{t.deadline}</span><span className="bid-final-label">{t.final}</span></div><div className="mt-2 font-mono text-4xl font-semibold tracking-[.08em] text-white sm:text-5xl">{timer}</div></div></div></section><section className="mt-5 grid gap-5 lg:grid-cols-[1fr_420px]"><div className="space-y-5"><div className="grid gap-3 sm:grid-cols-2"><PreviewInfo icon={<Radar className="h-5 w-5" />} label={locale === "zh" ? "運輸路線" : "Route"} value={t.route} /><PreviewInfo icon={<FileLock2 className="h-5 w-5" />} label={t.cargo} value={locale === "zh" ? "電子零件 · 500 kg · 3 CBM" : "Electronic components · 500 kg · 3 CBM"} /><PreviewInfo icon={<Clock3 className="h-5 w-5" />} label={locale === "zh" ? "運輸方式" : "Transport mode"} value={locale === "zh" ? "空運" : "Air freight"} /><PreviewInfo icon={<ShieldCheck className="h-5 w-5" />} label={t.services} value={locale === "zh" ? "清關、倉儲、香港本地派送" : "Customs, warehousing, local delivery"} /></div><Card><CardContent className="p-5"><div className="flex gap-3"><LockKeyhole className="mt-0.5 h-5 w-5 shrink-0 text-[#b18a25]" /><div><p className="font-semibold text-lblue">{t.sealed}</p><p className="mt-2 text-sm leading-6 text-slate-600">{locale === "zh" ? "你只可提交一次報價。其他參與者無法看見你的價格或條款。" : "You can submit one quote. Other participants cannot see your price or terms."}</p></div></div></CardContent></Card></div><Card className="bid-console h-fit border-[#d8b75a]/60"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold tracking-[.14em] text-[#9a7517]">{t.preview}</p><h2 className="mt-1 text-xl font-semibold text-lblue">{t.quote}</h2></div><LockKeyhole className="h-6 w-6 text-[#b18a25]" /></div>{sealed ? <div className="bid-success mt-6"><CheckCircle2 className="h-8 w-8" /><h3 className="mt-3 text-lg font-semibold">{t.success}</h3><p className="mt-2 text-sm leading-6">{t.successBody}</p><Button className="mt-5 w-full" variant="outline" onClick={() => setSealed(false)}>{t.edit}</Button></div> : <div className="mt-6 space-y-5"><PreviewField label={t.amount}><Input inputMode="decimal" value={amount} onChange={(event) => setAmount(event.target.value)} /></PreviewField><PreviewField label={t.transit}><Input value={transit} onChange={(event) => setTransit(event.target.value)} /></PreviewField><PreviewField label={t.terms}><Textarea value={terms} onChange={(event) => setTerms(event.target.value)} /></PreviewField><p className="flex items-center gap-2 text-xs font-medium text-slate-500"><ShieldCheck className="h-4 w-4 text-[#b18a25]" />{t.token}</p><Button className="bid-launch-button w-full" onClick={() => setConfirming(true)}>{t.launch}<ArrowRight className="h-4 w-4" /></Button></div>}</CardContent></Card></section>{confirming ? <div className="fixed inset-0 z-[80] grid place-items-center bg-[#101a36]/65 p-4 backdrop-blur-sm"><Card className="w-full max-w-md border-[#d8b75a]/55"><CardContent className="p-6"><p className="text-xs font-bold tracking-[.14em] text-[#9a7517]">{t.mission}</p><h2 className="mt-2 text-2xl font-semibold text-lblue">{t.confirm}</h2><div className="mt-5 space-y-3"><PreviewLine label={t.amount} value={`HKD ${Number(amount).toLocaleString()}`} /><PreviewLine label={t.transit} value={transit} /><PreviewLine label={locale === "zh" ? "傳輸狀態" : "Transmission"} value={locale === "zh" ? "加密封存" : "Encrypted and sealed"} /></div><div className="mt-6 flex gap-2"><Button className="flex-1" variant="outline" onClick={() => setConfirming(false)}>{t.edit}</Button><Button className="flex-1" variant="gold" onClick={() => { setConfirming(false); setSealed(true) }}>{t.confirm}</Button></div></CardContent></Card></div> : null}</section></main>
}
function PreviewInfo({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <Card><CardContent className="p-4"><div className="text-[#b18a25]">{icon}</div><p className="mt-3 text-xs font-medium text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-lblue">{value}</p></CardContent></Card> }
function PreviewField({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-sm font-semibold text-slate-700"><span className="mb-2 block">{label}</span>{children}</label> }
function PreviewLine({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between gap-4 border border-[#e4e9f1] bg-[#fafbfd] p-3 text-sm"><span className="text-slate-500">{label}</span><span className="text-right font-semibold text-lblue">{value}</span></div> }
